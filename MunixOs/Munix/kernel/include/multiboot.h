#ifndef MULTIBOOT_WRAPPER
#define MULTIBOOT_WRAPPER

/*
 * La implementación principal de multiboot ahora tiene su propia carpeta
 * esto se queda aqui por compatibilidad y como wrapper para incluir todo
 * de una vez.
 */

#include <stdint.h>
#include "../multiboot/multiboot.h"

#endif
